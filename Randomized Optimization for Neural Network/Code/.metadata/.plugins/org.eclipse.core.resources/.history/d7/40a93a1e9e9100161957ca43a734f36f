/**
 * 
 */
package optProblems;

import java.util.ArrayList;
import java.util.List;

import opt.EvaluationFunction;
import opt.HillClimbingProblem;
import opt.OptimizationAlgorithm;
import opt.OptimizationProblem;
import opt.SimulatedAnnealing;
import opt.example.TravelingSalesmanEvaluationFunction;
import shared.FixedIterationTrainer;

/**
 * @author zhihuixie
 *
 */
public class OptParams {
	OptimizationProblem op;
	EvaluationFunction ef;
	String algName;
	String paramName;
	public OptParams(OptimizationProblem op, EvaluationFunction ef, 
			        String algName, String paramName) {
		// TODO Auto-generated constructor stub
		this.op = op;
		this.ef = ef;
		this.algName = algName;
		this.paramName = paraName;
	}
	public List<Double> trainFunction(List<Integer> params1, List<Double> params2) {
		// TODO Auto-generated method stub
		/*
		List<Double> fitnessScore = new ArrayList<>();
		if (params1 != null){
			for (int i = 0; i < params1.size(); i++){
				if (this.name.equals("GA")){
					new SimulatedAnnealing(1E12, .95, hcp);
				}
	            FixedIterationTrainer fit = new FixedIterationTrainer(this.alg, 2000);
	            fit.train();
	            fitnessScore.add(this.ef.value(alg.getOptimal()));
				
			}
		}
		*/
		return null;
	}

}
